extends layout

//- block content
//-   h1= title
//-   //- - const umbrella = ['umbrella_type','color','cost']
//-   - let umbrella  = results;
//-   script.
//-     function buy(x){
//-       alert('Purchased Item: ' + x)
//-    } 
//-   div(class='container-fluid')
//-     div(class='row')
//-       each results, index in umbrella
//-         div(class='col-sm-2' onclick='buy('+index+')') #{results}



block content
  h1= title

  - let items = results; 

  - let o;
  script.
    function buy(x){
      alert('Purchased Item: ' + x)
   } 
  div(class='container-fluid')
    div(class='row')
      each o, index in items 
        div(class='col-sm-3' onclick='buy('+index+')') #{o} 